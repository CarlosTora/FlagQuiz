<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el botón está habilitado -->
    <item android:state_enabled="true" android:color="@color/bg_buttons" />

    <!-- Estado cuando el botón está deshabilitado (bloqueado) -->
    <item android:state_enabled="false" android:color="@color/bg_button_disable" />
</selector>